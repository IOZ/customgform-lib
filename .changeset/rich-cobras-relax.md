---
"@customgform-lib/react-customgform": minor
---

Added `theme` prop (`'light' | 'dark' | 'auto'`) to set the initial color scheme for forms with dark mode enabled. `'auto'` follows the visitor's system preference (default).
